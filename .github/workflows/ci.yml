name: Perl CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Cancel previous runs on the same PR
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}



# Minimal permissions by default
permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15  # Prevent hung jobs

    strategy:
      matrix:
        perl-version: ['5.30', '5.32', '5.34']

    steps:
    - uses: actions/checkout@v7

    - name: Set up Perl
      uses: shogo82148/actions-setup-perl@v1.42.0
      with:
        perl-version: ${{ matrix.perl-version }}

    - name: Install dependencies
      run: |
        # Install ExtUtils::MakeMaker first
        cpanm --quiet --notest ExtUtils::MakeMaker

        # Generate Makefile
        perl Makefile.PL

        # Install dependencies from Makefile
        cpanm --quiet --notest --installdeps .

        # Install test dependencies explicitly
        cpanm --quiet --notest Test::More Test::MockObject Test::Warn File::Temp

        # Install runtime dependencies
        cpanm --quiet --notest Net::SNMP File::Slurp Log::Log4perl Config::IniFiles Try::Tiny

    - name: Run unit tests
      run: prove -r t/unit

    - name: Run integration tests
      run: prove -r t/integration

  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 15  # Prevent hung jobs

    steps:
    - uses: actions/checkout@v7

    - name: Set up Perl
      uses: shogo82148/actions-setup-perl@v1.42.0
      with:
        perl-version: '5.34'

    - name: Install dependencies
      run: |
        cpanm --quiet --notest Perl::Critic
        cpanm --quiet --notest Perl::Tidy

    - name: Run Perl::Critic
      run: |
        perlcritic --quiet --severity 5 bin

    - name: Check Perl::Tidy
      run: |
        find bin -name "*.pm" -o -name "*.pl" | xargs perltidy -b
        if git diff --exit-code; then
          echo "Code is properly formatted"
        else
          echo "Code is not properly formatted"
          exit 1
        fi
